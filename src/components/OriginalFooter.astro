---
import { getOriginalFooter } from '../lib/legacyChrome';

interface Props {
  root?: string;
}

const { root = './' } = Astro.props;
const markup = getOriginalFooter(root);
---

<Fragment set:html={markup} />

