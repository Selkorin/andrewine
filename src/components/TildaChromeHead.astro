---
import { getTildaHeadAssets } from '../lib/legacyChrome';

const tildaAssets = getTildaHeadAssets();
---

<link rel="preconnect" href="https://fonts.gstatic.com" />
<Fragment set:html={tildaAssets} />
